---
name: user:seo-shop2
description: "Генерация полного SEO HTML-блока категории v2 (FAQ в Блоке 1, 5 вопросов, sameAs) для GOODMi. Используй, когда пользователь просит сгенерировать HTML-блок категории/модели для goodmi.ru."
---

Прочитай файл `.cursor/skills/seo-shop-page-builder-v2/SKILL.md` целиком (файл большой — если первое чтение обрежется, дочитай остаток через offset) и выполни описанный в нём workflow строго по шагам для запроса пользователя, переданного вместе с этой командой.

Обязательные блокирующие шаги — не пропускать:
- Step 1b: спросить гарантию, если не указана.
- Step 4: спросить URL картинок для карточек (или явное «пропустить»/«без фото») до вёрстки `.gm-services-grid`.

Сгенерируй два файла (`[slug].html` и `[slug]-jsonld.html`) и сохрани их в `Категории/Новые/`.
