services:
  portal:
    build: .
    container_name: goodmi-marketing-portal
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    env_file:
      - .env
    volumes:
      - portal-data:/app/data

volumes:
  portal-data:
